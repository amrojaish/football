# مشروع تطبيق الدوريات العربية

> **ملف سياق المشروع** — ارفعه مع أول رسالة بأي شات جديد
> آخر تحديث: 15 أغسطس 2026

**الموقع المنشور:** https://amrojaish.github.io/football/
**المستودع:** https://github.com/amrojaish/football
**مسار المشروع:** `C:\Users\User\Projects\Football`
⚠️ نُقل من `OneDrive\Desktop` — OneDrive كان يمنع Git من العمل ويسأل
256 مرة عن حذف مجلدات `.git`. **لا تُعد المشروع داخل OneDrive.**

---

## من أنا وما هذا المشروع

أنا **عمرو أبو جيش**، خريج BSc AI & Data Science من جامعة مانشستر متروبوليتان
(2026)، راجع للأردن وأبحث عن عمل في تحليل ومعالجة البيانات.

هذا المشروع **portfolio ومحاولة بزنس معاً**. بدأ كفكرة "تطبيق كرة قدم عالمي
بإشعارات أهداف"، ورُفضت الفكرة بشكلها الأصلي لأسباب واقعية:
- السوق مشبع (FotMob، SofaScore، 365Scores، Flashscore، كووورة)
- الداتا الحية غالية والمنافسون يملكون تغطية أوسع
- المشجع لا يدفع، والدعايات تحتاج ملايين المستخدمين

**فأُعيد تشكيلها:** التركيز على **الدوريات العربية** التي تغطيها التطبيقات
الكبرى بشكل ضعيف أو **خاطئ**. الميزة التنافسية ليست الداتا (مستأجرة من API)،
بل **طبقة التصحيح العربية** المبنية يدوياً فوقها.

**الدوريات المختارة ومنطق الاختيار:**

| الدوري | ID | لماذا |
|---|---|---|
| الأردني | 387 | السوق المحلي — يمكنني اختباره والتحقق منه شخصياً |
| العراقي | 542 | ~45 مليون نسمة، جمهور متعصب، تغطية رقمية ضعيفة |
| السعودي | 307 | جاذب ترافيك، نجوم عالميون، جمهور ممتد خارج البلد |

---

## الوضع الحالي (15 أغسطس 2026)

**مصدر الداتا:** API-Football (api-sports.io) — **خطة Pro مدفوعة (~$20/شهر)**
- 7,500 طلب/يوم · إعادة تعيين 00:00 UTC (03:00 بتوقيت الأردن)
- `SEASON = 2025` بـ`config.py`
- ⚠️ الـAPI يسمّي الموسم **بسنة بدايته**: 2025 = موسم 2025-26

**الداتا المخزّنة:**

| الدوري | موسم | مباريات | أندية | أهداف | بطاقات/تبديلات |
|---|---|---|---|---|---|
| الأردني | 2024 | 132 | 12 | 100% | ❌ لا يوفرها المزوّد |
| الأردني | 2025 | 135 | 10 | 100% | ❌ لا يوفرها المزوّد |
| العراقي | 2024 | 380 | 20 | 100% | ❌ لا يوفرها المزوّد |
| العراقي | 2025 | 380 | 20 | 100% | ❌ لا يوفرها المزوّد |
| **العراقي** | **2026** | **20** | **20** | جارٍ | ❌ لا يوفرها المزوّد |
| السعودي | 2024 | 306 | 18 | 100% | ✅ كاملة |
| السعودي | 2025 | 306 | 18 | 100% | ✅ كاملة |
| **السعودي** | **2026** | **303** | **18** | جارٍ | ✅ كاملة |

**الإجمالي:** 1960 مباراة · 63 نادي
(منها 7 مضافة يدوياً — محسومة إدارياً ولا وجود لها عند المزوّد،
و318 مباراة قادمة بنتيجة NULL)
**جدول `events`:** ~2680 بطاقة · ~4900 تبديل (السعودي فقط)

### ⚽ الموسم الجديد بدأ
- **الدوري السعودي 2026-27:** انطلق 13 أغسطس 2026، وينتهي 29 مايو 2027.
  الموسم كامل مجدول عند المزوّد (306 مباراة)، مع انقطاع من 24 ديسمبر
  إلى 7 فبراير لاستضافة كأس آسيا 2027.
  **الصاعدون:** أبها (2951) · الفيصلي (2930) · الدرعية (26738)
- **دوري نجوم العراق 2026-27:** انطلق 14 أغسطس 2026.
  **الصاعدون:** الجولان (28222) بطل الممتاز بـ80 نقطة — يصعد لأول
  مرة بتاريخه · غاز الشمال (28223) الوصيف بـ74 نقطة
- **الدوري الأردني:** لم يبدأ بعد

⚠️ **الفيصلي السعودي (2930) غير الفيصلي الأردني (4531)** — ناديان
   مختلفان بنفس الاسم العربي في دوريين مختلفين.

**ما أُنجز:**
- ✅ التحقق من 3 مزودي API (فقط API-Football يغطي الثلاثة بأحداث)
- ✅ جدول 58 نادي بأسماء ومدن عربية **مصححة يدوياً**
- ✅ نظام `.env` لحماية المفتاح + `.gitignore`
- ✅ قاعدة بيانات SQLite محلية (`football.db`)
- ✅ صفحة HTML بالعربي RTL مع **تبويبات موسم + دوري**
- ✅ نظام شعارات محلية يتجاوز أخطاء الـAPI (7 شعارات مصححة)
- ✅ سحب مقاوم لانقطاع الشبكة (retry + resume + budget)
- ✅ **الرفع على GitHub والنشر على GitHub Pages**
- ✅ **طبقة تصحيح النتائج** — `match_corrections.csv` + `apply_corrections.py`
  (مُختبرة: كُسرت النتيجة عمداً ورجّعها السكربت بنجاح)
- ✅ **طبقة استثناء المباريات** — `excluded_matches.csv` + `apply_exclusions.py`
- ✅ **طبقة دمج الأندية** — `team_merges.csv` + `apply_merges.py`
  (مُختبرة: كُسرت 39 مباراة عمداً ورجّعها السكربت)
- ✅ **ترتيب المتساويين بالمواجهات المباشرة** — السعودي، مطابق للرسمي
- ✅ **58 صفحة نادي** (`make_clubs.py`) بتبويبات مواسم
- ✅ **1639 صفحة مباراة** (`make_matches.py`) مع **شارة "نتيجة مصححة"**
- ✅ **البنية اكتملت:** دوري ← نادي ← مباراة، والتنقل يعمل بالاتجاهين
- ✅ **سحب البطاقات والتبديلات** — السعودي 607/607، صفر فشل
- ✅ **طبقة إضافة المباريات** — `manual_matches.csv` + `apply_manual.py`
  (لمباريات محسومة إدارياً لا وجود لها عند المزوّد إطلاقاً)
- ✅ **قائمة أحداث موحدة** بصفحة المباراة — أهداف وبطاقات وتبديلات
  مرتبة بالدقيقة، مع خياري "كل الأحداث" و"الأحداث المهمة"
- ✅ **إحصائيات الانضباط** (`discipline.py`) — أرقام لا يوفّرها أي API
- ✅ **واجهة صفحة النادي:** طي القوائم، فلترة المباريات بالنتيجة،
  ربط أسماء الأندية بصفحاتها
- ✅ **دعم اللغتين كاملاً** — `index.html` عربي و`en/index.html` إنجليزي،
  و63 نادياً و1960 مباراة × لغتين ≈ **4000 صفحة**
- ✅ **`i18n.py`** — كل نص في الموقع بمكان واحد، لا نص عربي داخل
  سكربتات التوليد
- ✅ **الأسماء الإنجليزية الرسمية** — عمود `name_en_official`،
  و**17 اسماً كان خاطئاً عند المزوّد**
- ✅ **المباريات القادمة** — `fetch_upcoming.py`، 318 مباراة مجدولة
- ✅ **الصفحة الرئيسية الجديدة** — قادمة ← نتائج ← بطاقات الدوريات
  ← الجداول

**التحقق الخارجي — ✅ ستة جداول من ستة:**

| الدوري | موسم | النتيجة | المصدر |
|---|---|---|---|
| الأردني | 2024 | ✅ مطابق 12/12 | **jfa.jo** (الاتحاد الأردني) |
| الأردني | 2025 | ✅ مطابق بعد تصحيح واحد | winwin |
| السعودي | 2024 | ✅ مطابق 18/18 بعد تصحيح واحد | تطبيق نتائج معتمد |
| السعودي | 2025 | ✅ مطابق 18/18 | تطبيق نتائج معتمد |
| العراقي | 2024 | ✅ مطابق 20/20 بعد دمج نوروز | تطبيق نتائج معتمد |
| العراقي | 2025 | ✅ مطابق 20/20 بعد **12 قراراً** | تطبيق نتائج معتمد |

**العراقي 2025 كان الأصعب:** 7 مباريات مضافة + 5 نتائج مصححة.
التفاصيل في درس 19.

⚠️ **لا تكتب "المصدر: الاتحاد السعودي/العراقي" على الموقع** — الصور جاءت
من تطبيق نتائج، لا من موقع الاتحاد. ادّعاء مصدر لم يُستعمل يهدم بالضبط
الشيء الذي يُفترض أن تبنيه الشارة.

---

## ⚠️ دروس تقنية حاسمة — لا تُنسَ

اكتُشفت بالتجربة وكلّفت وقتاً طويلاً. **قراءتها تمنع تكرار الكارثة.**

### 1. حد الطلبات بالدقيقة (أخطر اكتشاف)
الخطة المجانية ~10 طلبات/دقيقة (**غير معلن في التوثيق**). تجاوزه يُرجع
**رد HTTP 200 بمصفوفة فاضية بدون أي رسالة خطأ** — يبدو تماماً كأنه
"لا توجد بيانات لهذه المباراة".

هذا جعلنا نظن أن الـAPI لا يملك أحداثاً للدوريات العربية (تغطية 35%)،
وكدنا **نلغي فكرة المشروع كلها**. الحقيقة: كل الأحداث موجودة.

- خطة مجانية: `DELAY = 7.0` ثانية
- خطة Pro: `DELAY = 1.0` (مُختبر ويعمل)

### 2. الفشل الصامت — أخطر نمط برمجي
```python
return r.json().get("response", [])   # يبتلع كل الأخطاء
```
**أي دالة تجلب بيانات يجب أن تفحص الأخطاء وترجعها، لا أن تخفيها.**
الصيغة الصحيحة: `return (نجح؟, البيانات, سبب_الفشل)`

**إشارة تحذير مهمة:** الحصة استُهلكت 26 بدل 52 المتوقعة. فسّرتها
كخبر جيد. كانت في الحقيقة **إنذاراً** — الطلبات المرفوضة لا تُحتسب.

### 3. `Missed Penalty` ليست هدفاً
الـAPI يصنّفها `type = "Goal"`. رونالدو ظهر بهدفين وأحدهما ركلة ضائعة.
```python
if e.get("detail") == "Missed Penalty": continue
```

### 4. أهداف ملغاة بمباريات 0-0
تظهر بجدول الأحداث ولا تدخل بالنتيجة (تسلل/VAR)، وغالباً **بلا اسم لاعب**.
يعالجها `fix_goals.py`.

### 5. الفلاتر الناقصة (تكررت مرتين)
كل استعلام يحتاج `league_code AND season`. نقص أحدهما يخلط البيانات
**بصمت**. أول مرة كان الدوري، الثانية الموسم.

### 6. `INNER JOIN` يحذف بصمت
الأندية غير الموجودة بجدول `teams` **تختفي من النتائج بلا تحذير**.
ظهر جدول ترتيب بـ**8 فرق بدل 10** لهذا السبب — الفريقان الصاعدان
(البقعة، سما السرحان) لم يكونا بالجدول.

### 7. الشبكة غير موثوقة
انقطاعة واحدة أسقطت سحب 374 مباراة عند المباراة السادسة.
الحل: `try/except` + إعادة محاولة بتأخير متزايد + **تخطي** الفاشل
بدل الانهيار. (اختُبر فعلياً ونجح)

### 8. الدمج لا الاستبدال
أي سكربت يلمس ملفاً فيه شغل يدوي **يجب أن يدمج**. الاستبدال يمحو
ساعات عمل بصمت. (`update_teams.py`, `export_players.py`)
⚠️ `fetch_teams.py` القديم كان يستبدل — **حُذف لهذا السبب**.

### 9. مسار الداتا — التعديل لا يصل وحده
```
teams_arabic.csv → [sync_teams.py] → football.db → [make_site3.py] → index.html
```
أي تعديل على الـCSV يحتاج `sync_teams.py` بعده، ثم `make_site3.py`.

**وبعد أي سحب جديد:**
```
fetch_matches2.py → apply_exclusions.py → apply_corrections.py → make_site3.py
```
تخطّي هذه الخطوات يُرجع الأخطاء المصححة والمباريات المحذوفة يدوياً.

### 10. اختبار اكتمال الموسم
```
عدد المباريات = (مباريات كل فريق × عدد الأندية) ÷ 2
```

| الدوري | موسم | أندية | متوقع | لدينا |
|---|---|---|---|---|
| JOR | 2024 | 12 | 132 | 132 ✅ |
| JOR | 2025 | 10 | 135 (نظام 3 مراحل، 27 مباراة) | 135 ✅ |
| IRQ | 2025 | 20 | — | 373 ⚠️ (الفرق لعبت 35-38، غير متساوٍ) |
| SAU | 2025 | 18 | 306 | 306 ✅ |

### 11. f-string وأقواس CSS/JS
عند بناء HTML داخل f-string بايثون، كل `{` و`}` في CSS/JS يجب مضاعفتها.
**الحل الأنظف:** اجعل CSS وJS نصوصاً عادية خارج الـf-string وادمجها بالجمع.

### 12. الفراغات المخفية
النسخ واللصق في Excel يُدخل حرف tab مخفياً في بداية النصوص.
`.strip()` في القراءة يحل المشكلة لكل الأسماء دفعة واحدة.

### 13. الـAPI يعطي **نتيجة نهائية خاطئة** — لا أحداث ناقصة فقط
أخطر درس في المشروع كله.

مباراة `1538111` (الحسين إربد × الفيصلي، 2026-05-08 — **مباراة حسم اللقب**):
- الـAPI سجّلها **1-0** للحسين، بهدف باسم لاعب حقيقي، `detail = Normal Goal`
- النتيجة الرسمية **0-0** — الهدف أُلغي بالـVAR

**لماذا فشلت كل الفلاتر الداخلية:**
الخطأ في `fixture score` نفسه، وجدول `goals` **متطابق معه**. فحص
"هل عدد الأهداف = النتيجة؟" يمر بنجاح. فحص "هدف بلا اسم لاعب" يمر.
فحص `Missed Penalty` يمر. **لا يوجد تناقض داخلي يمكن اكتشافه بكود.**

**كيف اكتُشف:** مقارنة خارجية **عمود بعمود** — لا بمقارنة النقاط وحدها:

| | عندنا | الرسمي | الفرق |
|---|---|---|---|
| الحسين | ف19 ت5 خ3 · له 70 | ف18 ت6 خ3 · له 69 | هدف زائد |
| الفيصلي | ت5 خ5 · عليه 25 | ت6 خ4 · عليه 24 | هدف زائد عليه |

الفرق `−2` للحسين و`+1` للفيصلي بدا صغيراً، لكنه انحلّ لهدف واحد.
**فرضية "ترحيل نقاط منقوص بين المراحل" كانت خاطئة تماماً** — لو كانت
صحيحة لأثّرت على كل الفرق بفرق كبير وباتجاه واحد.

**القاعدة:** قارن `P/W/D/L/GF/GA` صفاً بصف. النقاط رقم مُجمَّع يخفي المصدر.

### 14. `league_id` يحتوي مباريات من خارج الدوري
مباراة `1550045` (بغداد × كربلاء، 2026-06-08) ظهرت تحت `league 542`:
- **بعد نهاية الموسم الرسمي بعشرة أيام** (انتهى 28 مايو 2026)
- **كربلاء هابط للدرجة الأولى** ولا يشارك في الموسم أصلاً

النتيجة: نادٍ زائد بجدول الترتيب (21 بدل 20). عولجت بـ`excluded_matches.csv`.

**اختبار الكشف:** نادٍ بعدد مباريات شاذ (1 مقابل 35+) = علامة إنذار.
يفحصه `diagnose_irq.py`.

### 15. أخطاء الاتجاه (RTL) — احذر التشخيص الخاطئ
عمود `+/-` يظهر بصرياً `63+` بدل `+63`. السبب حقيقي: خوارزمية Bidi
(UAX #9) تصنّف `+` كـES، ولا تأخذ اتجاه الرقم إلا إذا كانت **بين** رقمين.

⚠️ **لكن:** جُرّب `dir="ltr"` على نتائج المباريات (`class="score"`)
فقلبت النتائج إلى الخطأ. الترتيب البصري الحالي **صحيح**، والتعديل
أُلغي. الدرس: أي إصلاح RTL يجب اختباره بصرياً قبل اعتماده،
والافتراض النظري وحده لا يكفي.

### 16. ترتيب المتساويين لا يُحسب بفارق الأهداف
الدوريات الثلاثة ترتّب المتساويين بالنقاط عبر **المواجهات المباشرة**
أولاً، لا بفارق الأهداف الكلي.

مثال مؤكد (السعودي 2025-26):
```
الفتح   37 نقطة · فارق -14 · المركز 11
الخليج  37 نقطة · فارق -8  · المركز 12
```
الفارق الأفضل لا يعني مركزاً أعلى.

**لا يُكتب بـSQL بشكل نظيف** — يحتاج: تجميع المتساويين، حساب مواجهاتهم،
ترتيبهم داخل المجموعة، ثم إعادة دمجهم. لذلك `tiebreak.py` وحدة منفصلة.

**القواعد الرسمية:**

| الدوري | الترتيب | الثقة |
|---|---|---|
| الأردني | نقاط ← نقاط المواجهات ← فارق المواجهات ← أهداف المواجهات ← فارق كلي ← أهداف كلية | [مؤكد] — المادة 13، لائحة بطولات الاتحاد الأردني |
| السعودي | نقاط ← نقاط المواجهات ← فارق المواجهات ← أهداف المواجهات ← فارق كلي ← أهداف كلية | [مؤكد] — تحقق خارجي |
| العراقي | نقاط ← نقاط المواجهات ← فارق المواجهات ← **فارق كلي** ← أهداف كلية | [مرجح] — IPFL عبر ويكيبيديا |

⚠️ **فرق تقني:** العراقي ينتقل لفارق الأهداف الكلي بعد فارق المواجهات
مباشرة — بدون خطوة "أهداف المواجهات" الموجودة بالأردني والسعودي.

⚠️ **السعودي يفرّق بين الترتيب أثناء الموسم والترتيب النهائي:** حتى
الجولة قبل الأخيرة يُرتَّب بفارق الأهداف الكلي، وبالنهاية بالمواجهات.
كودنا يطبّق قاعدة النهاية (الموسم مكتمل 34/34).

**المفعّل حالياً: السعودي فقط.** الأردني والعراقي معطّلان في
`H2H_ENABLED` بـ`tiebreak.py` للأسباب التالية:
- **الأردني:** نظام 3 مراحل — عدد المواجهات بين الفرق قد يختلف،
  ولا يوجد حالياً أي تعادل نقاط بالجدول (لا أثر عملي)
- **العراقي:** القاعدة تشترط اكتمال كل المواجهات بين الفرق المعنية،
  والموسم غير مكتمل (35-38 مباراة)

### 17. تغطية الأحداث تختلف جذرياً بين الدوريات
الأهداف متوفرة 100% للدوريات الثلاثة. **البطاقات والتبديلات للسعودي فقط.**

| الدوري | العيّنة | بطاقات | تبديلات |
|---|---|---|---|
| السعودي | 607 مباراة | **2663** | **4869** |
| الأردني | 135 مباراة (كاملة) | 26 | **0** |
| العراقي | 5 مباريات | 1 | 0 |

**[مؤكد]** الأردني مفحوص على الموسم كاملاً لا على عيّنة. المزوّد لا يوفّر
هذه الأحداث لهما — ليست مشكلة في السحب.

**قرار التصميم:** قسم البطاقات **يختفي** عند غياب الداتا، ولا يُعرض فارغاً.
القسم الفارغ يوحي بأن المباراة بلا بطاقات، وهذا غير صحيح — وهو أسوأ من
عدم العرض أصلاً. (هذا ما تفعله التطبيقات الكبرى ولا تشرحه)

**توفير:** انتُخطي الأردني والعراقي عمداً — 1500 طلب مقابل داتا شبه معدومة.

### 18. OneDrive و Git لا يعملان معاً
كان المشروع في `OneDrive\Desktop`. عند رفع 1632 ملفاً، منع OneDrive
حذف مجلدات `.git/objects` المؤقتة وسأل **256 مرة** عن إعادة المحاولة.
الخطر الحقيقي: احتمال فساد المستودع وضياع تاريخ المشروع.

**الحل:** نُقل المشروع إلى `C:\Users\User\Projects\Football` (خارج OneDrive).
`.git` جزء من المجلد فانتقل التاريخ كاملاً.

⚠️ **لا تستخدم "Choose folders" في إعدادات OneDrive لإخفاء Desktop** —
التحذير صريح: الملفات ستُحذف من الجهاز. النقل هو الحل الصحيح.

**ملاحظة:** رسالة `Deletion of directory '.git/objects/XX' failed` ما زالت
تظهر أحياناً بعد النقل (مضاد فيروسات على الأرجح). **الـcommit ينجح** —
الفشل في التنظيف فقط. اضغط `n` أو `Ctrl+C`، ثم تحقق بـ`git status`.

### 19. مباريات لا وجود لها عند المزوّد إطلاقاً
أعقد مشكلة داتا في المشروع، وحلّها استغرق جلسة كاملة.

**العراقي 2025 كان ناقصاً 7 مباريات** — لا كنتيجة ولا كسجل. السبب:
مباريات أُلغيت أو لم تُقَم، وحُسمها الاتحاد إدارياً 3-0، فالـAPI
لا يسجّلها من الأساس. النتيجة: فرق لعبت 35 و36 و37 بدل 38.

**لماذا لم تكفِ `match_corrections.csv`:** هي تصحّح نتيجة موجودة.
هنا لا يوجد ما يُصحَّح — يجب **إضافة** المباراة. لذلك
`manual_matches.csv` بمعرّفات تبدأ من 9000001 لتمييزها عن الـAPI.

**المنهج الذي نجح (والذي فشل):**

❌ **لا** تجمع قرارات لجنة الانضباط من الأخبار وتطبّقها. القرارات
تُعدَّل بالاستئناف والأخبار توثّق **لحظة صدورها لا حالتها النهائية**.
حالة الطلبة × النفط تقلّبت **ثلاث مرات**. وخبر عن انسحاب القاسم
أمام القوة الجوية تبيّن أنه من **موسم 2023-24** لا موسمنا — كاد
يُفسد مباراة صحيحة.

✅ **المنهج الصحيح — عكسي وثلاثي المراحل:**
1. `find_missing.py` — أي أزواج لم تتقابل العدد المطلوب من المرات
   (بالاسم لا بالعدد فقط)
2. `solve_missing.py` — يجرّب كل التركيبات ويجد ما يصفّر الفروقات
3. `match_draws.py` — بعد إضافة الناقص، يبحث في التعادلات الموجودة
   عن التي تحوّلت لحسم إداري

**النتيجة:** 71 خانة مختلفة ← صفر. المصادر أكّدت 4 من 7، والحساب
حسم الباقي، ثم كشف **5 نتائج خاطئة** في مباريات موجودة.

⚠️ **الحل العددي وحده لا يكفي.** حين تُركت المباريات السبع للحساب
بلا تثبيت، اقترح `القاسم 3-0 الموصل` — عكس ما تقوله المصادر تماماً.
السكربت يقلّل الخطأ الكلي، ولا يعرف الحقيقة. **ثبّت المؤكد من
المصادر أولاً، ثم دع الحساب يحل الباقي.**

### 20. المعادلة ذات الحلول المتعددة
عند إدخال الجدول الرسمي من صورة لا تعرض `ف/ت/خ`، لا تستنتجها من
`3ف + ت = نقاط` — **للمعادلة أكثر من حل**. النجف بـ25 نقطة يصلح
أن يكون (6ف، 7ت) أو (7ف، 4ت).

استنتجتُ القيم فبدت النجف والقاسم "مختلفَين" عن الرسمي بينما
**أهدافهما مطابقة تماماً**. الأعمدة المحسوبة من قاعدة البيانات
كانت الصحيحة، والاستنتاج هو الخطأ.

**القاعدة:** إن لم يكن العمود ظاهراً في المصدر، اتركه ولا تخمّنه.

### 21. تعارض أسماء الكلاسات في CSS
فلترة المباريات لم تعمل، والسبب: الكلاس `l` (خسارة) يتعارض مع
`<div class="l">فاز</div>` المستخدَم لعناوين الإحصائيات. المتصفح
لا يعطي خطأ — يطابق العنصر الخطأ بصمت.

**الحل:** بادئة مميزة — `rw` / `rd` / `rl` بدل `w` / `d` / `l`.

**القاعدة:** الكلاسات ذات الحرف الواحد خطيرة في ملف واحد كبير.

### 22. أسماء الأندية الإنجليزية عند المزوّد خاطئة أيضاً
بعد تصحيح الأسماء العربية، تبيّن أن الإنجليزية بها **17 خطأ من 58**:

| المزوّد | الرسمي | ملاحظة |
|---|---|---|
| `Baghdad` | **Amanat Baghdad** | نفس الخطأ المكتشف بالعربية |
| `Al Wihdat` | **Al-Wehdat** | |
| `Aqaba` | **Shabab Al-Aqaba** | الاسم الرسمي فيه Shabab |
| `Naft Al-Janoob` | **Naft Al-Basra** | النادي غيّر اسمه |
| `Al-Qadisiyah FC` | **Al-Qadsiah** | النادي يكتبها هكذا |
| `Al Minaa Basra` | **Al-Minaa** | بلا Basra |
| `Al Khaleej Saihat` | **Al-Khaleej** | بلا Saihat |

**البنية:** عمود `name_en_official` جديد، و`name_en` يبقى كمرجع —
نفس فلسفة `logo` و`logo_local`. الاستعلامات تستخدم:
```sql
COALESCE(NULLIF(t.name_en_official,''), t.name_en)
```
فأي نادٍ بلا اسم رسمي يرتد لاسم المزوّد تلقائياً.

**السجل:** `club_names_review.csv` فيه لكل نادٍ: اسم المزوّد، الاسم
الرسمي، **مستوى الثقة**، والمصدر. الأسماء بمستوى [تخميني] تُترك فارغة.

### 23. المباريات القادمة تكسر حسابات الجدول
`fetch_matches2.py` يسحب `status=FT` فقط، فالمباريات القادمة لم تكن
موجودة إطلاقاً. بعد سحبها بـ`fetch_upcoming.py` انكسر كل شيء:

```
TypeError: bad operand type for unary -: 'NoneType'
```

**السبب:** نتيجة المباراة القادمة `NULL`، فـ`SUM(gf)` ترجع `None`
للفرق التي لم تلعب بعد، و`tiebreak.py` يحاول `-None`.

**الحل — ثلاث نقاط، كلها ضرورية:**
1. **حساب الجدول:** أضف `AND home_goals IS NOT NULL` لكل استعلام
   ترتيب (في `make_site3.py` و`make_clubs.py`)
2. **بطاقات المباريات:** افحص `None` قبل مقارنة النتيجة، واعرض `—`
3. **صفحة المباراة:** اعرض "مباراة قادمة" و"لم تبدأ بعد" بدل
   `None - None` و"انتهت بالتعادل السلبي"

⚠️ **ونادٍ لم يلعب بعد لا يظهر في جدول الترتيب إطلاقاً** — يجب أن
تعمل صفحته رغم ذلك (إحصائيات صفرية ومركز `—`)، وإلا اختفت صفحات
الأندية الصاعدة كلها.

**التكلفة:** `fetch_upcoming.py` يستهلك **طلباً واحداً لكل دوري**
لأنه لا يجلب أحداثاً — أرخص بمئات المرات من `fetch_matches2.py`.

### 24. ترتيب المباريات يعتمد على نوعها
المباريات المنتهية تُرتَّب **الأحدث أولاً** (آخر نتيجة فوق).
المباريات القادمة تُرتَّب **الأقرب أولاً** (أقرب موعد فوق).

عرضهما بترتيب واحد يعني أن المستخدم ينزل للأسفل ليرى مباراة الغد.

**وعدّاد أزرار "عرض الكل" يجب أن يعرض الباقي لا الإجمالي** — زر
مكتوب عليه `(34)` بينما 3 معروضة أصلاً يربك المستخدم؛ الصحيح `(31)`.
وعند الفلترة يجب أن يعرض عدد المفلتر لا عدد الكل.

---

## أخطاء الـAPI المكتشفة (نمط متكرر)

| الخطأ | التفاصيل | الحالة |
|---|---|---|
| **نتيجة مباراة حسم اللقب** | 1538111: 1-0 والصحيح 0-0 | صُحح عبر `match_corrections.csv` |
| **نتيجة العروبة × النصر** | 1252618 (2025-02-28): 2-1 والصحيح 0-3 | صُحح عبر `match_corrections.csv` |
| **7 مباريات مفقودة** | العراقي 2025: محسومة إدارياً 3-0، لا سجل لها | أُضيفت عبر `manual_matches.csv` |
| **5 نتائج محسومة إدارياً** | العراقي 2025: تعادلات بالملعب صارت 3-0 | صُححت عبر `match_corrections.csv` |
| **17 اسماً إنجليزياً خاطئاً** | من 58 نادياً — Baghdad، Al Wihdat، Aqaba... | صُححت عبر `name_en_official` |
| ترميز HTML بالمدن | `Al Majma&apos;ah` للفيصلي السعودي | يُنظَّف يدوياً |
| **مباراة خارج الدوري** | 1550045: كربلاء (هابط) بعد نهاية الموسم | استُثنيت عبر `excluded_matches.csv` |
| **نوروز بمعرّفين** | 6689 (11 مباراة) + 25062 (27) بموسم 2024 | دُمج عبر `team_merges.csv` |
| **أمانة بغداد باسم مختصر** | الـAPI يسمّيه `Baghdad` (11069) | صُحح الاسم العربي بالـCSV |
| نوروز مكرر | سجلان: 6689 و 25062 | حُذف المكرر |
| نوروز — المدينة | Stockholm بدل دهوك | صُحح |
| الصريح — المدينة | الرمثا بدل إربد | صُحح (تحقق من مصادر) |
| سما السرحان — المدينة | الرمثا بدل المفرق | صُحح |
| الأهلي الأردني — الشعار | يعرض شعار الأهلي السعودي القديم | صُحح محلياً |
| الخليج — المدينة | الدمام بدل سيهات | صُحح |
| ترميز HTML | `Ba&apos;quba` | نُظّف |
| تشكيل لاتيني | `al-Basra`, `Sakaka` | نُظّف |

**النمط:** أخطاء منهجية بالدوريات العربية — خلط بين أندية متشابهة الاسم
(الأهلي، نوروز، السرحان)، **ونتائج نهائية خاطئة**، **ومباريات من خارج
الدوري**. **هذا بالضبط ما يبرر وجود المشروع** — لو كانت داتاهم مضبوطة
لما كان هناك مبرر.

---

## هيكل الملفات

```
Football/
├── .env                    # API_FOOTBALL_KEY=xxx  (مستثنى من Git)
├── .gitignore              # يحمي .env والملفات المولّدة
├── config.py               # إعدادات + قراءة .env + SEASON + LEAGUES
├── football.db             # SQLite (مستثنى من Git)
├── teams_arabic.csv        # ⭐ الأصل الأهم — 58 نادي (تراكمي)
├── match_corrections.csv   # ⭐ تصحيحات نتائج يدوية (تراكمي)
├── excluded_matches.csv    # ⭐ مباريات من خارج الدوري (تراكمي)
├── team_merges.csv         # ⭐ دمج سجلات الأندية المكررة (تراكمي)
├── manual_matches.csv      # ⭐ مباريات غير موجودة عند المزوّد (تراكمي)
├── club_names_review.csv   # ⭐ سجل الأسماء الإنجليزية ومصادرها
├── players_arabic.csv      # أسماء اللاعبين (91 لاعب، فارغ الترجمة)
├── index.html              # المخرَج المنشور (عربي)
├── en/                     # ⭐ النسخة الإنجليزية كاملة
│   ├── index.html
│   ├── clubs/
│   └── matches/
├── clubs/                  # 63 صفحة نادي (مولّدة)
├── matches/                # 1960 صفحة مباراة (مولّدة)
├── logos/                  # شعارات محلية (اسم الملف = team_id)
├── archive/                # كود متقاعد
│
│  --- السحب ---
├── fetch_matches2.py       # ⭐ سحب تزايدي — يقرأ الاستثناءات، يدعم --season
├── fetch_events.py         # ⭐ سحب البطاقات والتبديلات (لا يلمس matches/goals)
├── fetch_upcoming.py       # ⭐ سحب المباريات القادمة (طلب واحد لكل دوري)
├── add_events_table.py     # إنشاء جدول events (مرة واحدة)
├── refetch_events.py       # إعادة سحب الأحداث الناقصة
├── update_teams.py         # ⭐ إضافة أندية جديدة (دمج آمن)
├── build_db.py             # السكربت الأصلي (قديم، لا يُستخدم)
│
│  --- معالجة الداتا ---
├── sync_teams.py           # ⭐ نقل CSV → DB
├── apply_merges.py         # ⭐ دمج الأندية المكررة (بعد كل سحب)
├── apply_manual.py         # ⭐ إضافة المباريات المفقودة (بعد كل سحب)
├── apply_club_names.py     # تطبيق الأسماء الإنجليزية الرسمية
├── apply_exclusions.py     # ⭐ حذف مباريات خارج الدوري (بعد كل سحب)
├── apply_corrections.py    # ⭐ تطبيق تصحيحات النتائج (بعد كل سحب)
├── fix_goals.py            # حذف Missed Penalty والأهداف الملغاة
├── clean_logos_all.py      # إزالة الخلفية البيضاء (flood fill)
├── export_players.py       # استخراج اللاعبين للترجمة
│
│  --- الفحص ---
├── audit.py                # ⭐ تغطية الأحداث + جودة الداتا
├── verify_standings.py     # ⭐ جدول ترتيب مفصّل للمقارنة الخارجية
├── check_h2h.py            # مواجهات مباشرة + تفاصيل أهداف كل مباراة
├── diagnose_irq.py         # أندية زائدة + خلط مواسم + توزيع شهري
├── find_missing.py         # ⭐ أي مواجهات ناقصة (بالاسم لا بالعدد)
├── discipline.py           # ⭐ إحصائيات الانضباط (السعودي فقط)
├── check_points.py         # تشخيص فروقات النقاط
├── quota.py                # الحصة المتبقية
├── investigate.py          # تحقيق بأنواع الأهداف
├── stats.py                # إحصائيات محسوبة (سلاسل، توزيع أهداف)
├── read_db.py              # عرض بالتيرمنال
│
│  --- العرض ---
├── i18n.py                 # ⭐ كل نصوص الموقع بالعربي والإنجليزي
├── tiebreak.py             # ⭐ ترتيب المتساويين (H2H) — يستورده make_site3
├── make_clubs.py           # ⭐ توليد صفحات الأندية (58)
├── make_matches.py         # ⭐ توليد صفحات المباريات (1632)
└── make_site3.py           # ⭐ توليد index.html
```

### أعمدة `teams_arabic.csv`
```
league_code | league_ar | team_id | name_en | name_en_official
name_ar | short_name_ar | city | logo | logo_local | logo_note
```
- `name_en` — **اسم المزوّد**، يُحتفظ به كمرجع ولا يُعرض
- `name_en_official` — ⭐ الاسم الإنجليزي الرسمي، **هذا المعروض**
- `short_name_ar` — الاسم العربي المختصر، **هذا المعروض**
- `logo_local` — مسار شعار محلي يتجاوز شعار الـAPI الخاطئ
- `logo_note` — توثيق سبب التصحيح (لم يعد يُعرض في الموقع)

### أعمدة `club_names_review.csv`
```
team_id | league | current_api_name | official_english | confidence | note
```
- `confidence` = مؤكد / مرجح / تخميني
- **[تخميني] لا يُطبَّق** — يُترك فارغاً ويرتد لاسم المزوّد

### أعمدة `match_corrections.csv`
```
match_id | league_code | season | correct_home | correct_away | note | source
```
- `note` — سبب التصحيح (مثال: "هدف د.13 ملغي — النتيجة الرسمية 0-0")
- `source` — من أين جاءت النتيجة الصحيحة (سجل شفاف)
- إذا كانت النتيجة الصحيحة 0-0، السكربت يحذف أهداف المباراة تلقائياً

### أعمدة `excluded_matches.csv`
```
match_id | league_code | season | reason | source
```
- للمباريات التي وضعها الـAPI تحت `league_id` خاطئ

### أعمدة `team_merges.csv`
```
old_id | new_id | league_code | note | source
```
- لما يملك الـAPI معرّفين لنفس النادي، فيظهر مرتين بأرقام ناقصة

### أعمدة `manual_matches.csv`
```
match_id | league_code | season | date | home_name | away_name
home_goals | away_goals | note | source
```
- **المعرّفات تبدأ من 9000001** لتمييزها عن معرّفات الـAPI
- الأسماء تُطابَق مع `short_name_ar` — لا معرّفات، أسهل بالكتابة اليدوية
- بعض التواريخ تقديرية وموثّقة بذلك في `note`
- هذه المباريات بلا أحداث في `goals` — صحيح، فهي لم تُلعب

### أعمدة `players_arabic.csv`
```
league_code | team_id | team_ar | player_en | player_ar | goals
```

### جداول `football.db`
```sql
teams   (team_id, league_code, name_en, name_ar,
         short_name_ar, city, logo)
matches (match_id, league_code, season, date,
         home_id, away_id, home_goals, away_goals, status)
goals   (id, match_id, team_id, minute,
         player_en, player_ar, detail)
events  (id, match_id, team_id, minute, type, detail,
         player_en, player_ar, assist_en)
```
⚠️ عمود `player_ar` **فارغ بالكامل** في الجدولين — هذا عمل مؤجل.

**جدول `events`:** منفصل عن `goals` عمداً — `goals` له منطق تنظيف خاص
(`fix_goals.py`) وخلطه بالبطاقات كان سيعقّد كل استعلامات الهدافين.
- `type` = `Card` / `subst` / `Var` / `none`
- `detail` = `Yellow Card` / `Red Card` / `Substitution 1` ...
- `assist_en` = اللاعب الخارج في التبديل
- `type='none'` = سجل فارغ يمنع إعادة سحب مباراة بلا أحداث

---

## الأوامر المستخدمة

```bash
# الفحص
python quota.py                          # الحصة المتبقية
python audit.py                          # جودة الداتا وتغطية الأحداث
python stats.py IRQ                      # إحصائيات محسوبة
python verify_standings.py JOR 2025      # جدول مفصّل للمقارنة الخارجية
python check_h2h.py الحسين الفيصلي        # مواجهات مباشرة
python diagnose_irq.py                   # أندية زائدة/خلط مواسم

# السحب
python fetch_matches2.py IRQ --check     # فحص قبل السحب
python fetch_matches2.py IRQ --budget 400
python fetch_matches2.py SAU --season 2024 --budget 300
python fetch_events.py --check           # كم مباراة بلا أحداث
python fetch_events.py SAU --budget 700  # بطاقات وتبديلات (السعودي فقط)
python fetch_upcoming.py --check         # المباريات القادمة (طلب/دوري)
python fetch_upcoming.py                 # ⭐ سحبها
python refetch_events.py JOR --budget 40
python update_teams.py 2025 --check      # أندية جديدة

# المعالجة
python sync_teams.py                     # CSV → DB (ضروري بعد تعديل CSV)
python apply_merges.py --check           # فحص الدمج
python apply_merges.py                   # ⭐ بعد كل سحب
python apply_manual.py --check           # فحص المباريات المضافة
python apply_manual.py                   # ⭐ بعد الاستثناءات
python apply_club_names.py --check       # فحص الأسماء الإنجليزية
python apply_club_names.py               # تطبيقها
python apply_exclusions.py --check       # فحص الاستثناءات
python apply_exclusions.py               # ⭐ بعد الدمج
python apply_corrections.py --check      # فحص التصحيحات
python apply_corrections.py              # ⭐ بعد الاستثناءات
python fix_goals.py                      # تنظيف الأهداف الخاطئة
python clean_logos_all.py                # معالجة الشعارات

# العرض
python make_site3.py                     # توليد index.html
python make_clubs.py                     # توليد صفحات الأندية
python make_matches.py                   # توليد صفحات المباريات

# إحصائيات وتشخيص
python discipline.py SAU 2025            # إحصائيات الانضباط
python find_missing.py IRQ 2025          # أي مواجهات ناقصة
python peek.py                           # فحص بنية HTML المولّد

# النشر (بعد أي تعديل)
git add .
git commit -m "وصف التغيير"
git push
```

### الترتيب الإجباري بعد أي سحب جديد
```
fetch_matches2.py   (المنتهية)
fetch_upcoming.py   (القادمة)
   ↓
apply_merges.py        ← وحّد الأندية المكررة أولاً
   ↓
apply_exclusions.py    ← احذف ما ليس من الدوري
   ↓
apply_manual.py        ← أضف المباريات المفقودة
   ↓
apply_corrections.py   ← صحّح ما تبقّى
   ↓
make_site3.py  +  make_clubs.py  +  make_matches.py
```
المنطق: وحّد الأندية، ثم احذف الزائد، ثم أضف الناقص، ثم صحّح الباقي.
لا داعي لتصحيح مباراة ستُحذف، ولا لإضافة واحدة قبل توحيد الأسماء.

⚠️ `fetch_events.py` **لا يحتاج هذه السلسلة** — لا يلمس `matches` ولا `goals`.

### عند بداية موسم جديد
```
1. python fetch_upcoming.py --season 2026     ← يحذّر من الأندية المفقودة
2. python update_teams.py 2026 SAU            ← أضف الصاعدين
3. عبّئ name_ar و short_name_ar و name_en_official في teams_arabic.csv
4. python sync_teams.py
5. python make_site3.py + make_clubs.py + make_matches.py
```
⚠️ **بدون الخطوة 3، الأندية الجديدة تختفي من الجداول** (درس 6).

---

## مبادئ متفق عليها

1. **التحقق قبل البناء** — لا يُكتب كود قبل إثبات توفر الداتا
2. **الأصول اليدوية هي الميزة** — الأسماء والشعارات والتصحيحات لا تُنسخ بسهولة
3. **فصل البيانات عن المنطق** — الكود يعرف *كيف*، والداتا تقول *ماذا*
4. **الديتابيس المحلية أولاً** — العمل على الداتا لا يستهلك الحصة
5. **توثيق التصحيحات لا محوها** — `logo_note` و`note`/`source` سجل شفاف
6. **أي نتيجة مخالفة للتوقع تستحق تفسيراً** — حتى لو بدت إيجابية.
   الشذوذ الصغير المُهمَل أخفى أكبر باگ بالمشروع.
7. **صلّح المصدر لا النتيجة** — التنظيف وحده علاج للعَرَض؛
   يجب تعديل مسار الإدخال أيضاً.
8. **لا تعديل يدوي على `football.db` مباشرة** — كل تصحيح يجب أن يكون
   في ملف CSV يُعاد تطبيقه. التعديل المباشر يضيع مع أول سحب جديد.
9. **المقارنة الخارجية إجبارية قبل النشر** — لا يوجد فحص داخلي يكشف
   نتيجة خاطئة متسقة مع نفسها. (درس 13)
10. **لا يُكتب منطق مبني على قاعدة غير مؤكدة** — قواعد الترتيب تختلف
    بين الدوريات. التعطيل الصريح أفضل من التطبيق التخميني. (درس 16)
11. **اختبر السحب على عيّنة قبل الالتزام** — 5 مباريات كشفت أن الأردني
    والعراقي بلا بطاقات، ووفّرت 1500 طلب و25 دقيقة. (درس 17)
12. **لا تدّعِ مصدراً لم تستعمله** — كتابة "المصدر: الاتحاد العراقي"
    والمقارنة تمت مع تطبيق نتائج، تهدم بالضبط ما تبنيه الشفافية.
13. **ثبّت المؤكد قبل أن تحسب** — الحل العددي يقلّل الخطأ الكلي ولا
    يعرف الحقيقة. اترك له ما لا مصدر له فقط. (درس 19)
14. **تحقّق من تاريخ كل خبر** — خبر انسحاب القاسم كان من موسم سابق
    وكاد يُفسد مباراة صحيحة. الأندية تكرّر أزماتها عبر المواسم.
15. **لا تخمّن عموداً غير ظاهر في المصدر** — للمعادلة أكثر من حل،
    والقيمة المحسوبة من الداتا أوثق من المستنتجة. (درس 20)
16. **اسم المزوّد يُحفظ ولا يُمحى** — `name_en` بجانب `name_en_official`،
    و`logo` بجانب `logo_local`. الأصل مرجع للمقارنة المستقبلية.
17. **لا تعرض قسماً فارغاً** — البطاقات الفارغة توحي بمعلومة خاطئة
    (مباراة بلا بطاقات، موسم بلا مباريات). الإخفاء أصدق. (درسا 17 و23)

---

## أسلوب العمل المطلوب من المساعد

- **اللغة:** عامية أردنية/شامية، والمصطلحات التقنية بالإنجليزي
- **مستواي:** خريج AI & Data Science نظرياً، لكن **مبتدئ عملياً بالبرمجة**.
  أحتاج خطوات تفصيلية جداً: أين أضغط، ماذا أكتب بالتيرمنال، كيف أحفظ الملف.
- **الأدوات:** VS Code + PowerShell على ويندوز 11
- **الأسلوب المطلوب:** صراحة مباشرة، **الحقيقة المزعجة أولاً**،
  بدون مجاملة أو عبارات مدح فارغة. إذا كانت فكرتي خاطئة قل ذلك بالسطر الأول.
- **مستويات الثقة:** [مؤكد] / [مرجح] / [تخميني] — استخدمها في الادعاءات
- **لا تخمّن أسماء اللاعبين أو الأندية** — الدقة أهم من السرعة،
  والاسم المخمّن الخاطئ يدمّر مصداقية المشروع كله
- **الأوامر الطويلة تُكتب في ملف .py لا في التيرمنال** — PowerShell
  يفسّر أجزاء من أوامر `python -c` الطويلة كأوامر خاصة به ويفشل
- **عند تعديل سطر: اكتب لي السطر النهائي كاملاً** جاهزاً للاستبدال،
  لا جزءاً يُلصق فوق القديم

---

## ملاحظات على الأدوات (مصائد معروفة)

- **`verify_standings.py` لا يستخدم `tiebreak.py`** — له ترتيب داخلي خاص
  للمتساوين. عند اختلافه عن الموقع، **الموقع هو الصحيح** (تأكّد ذلك في
  العراقي 2024: الكرخ/النجف عند 46 نقطة).
- **`check_h2h.py` مكتوب لنظام الأردني بثلاث مراحل** — تحذيره "المتوقع
  3 مواجهات" **خاطئ** للسعودي والعراقي (ذهاب وإياب = مواجهتان). تجاهله.
- **`fetch_events.py` يُدخل سجلاً بـ`type='none'`** للمباريات التي لا
  أحداث لها فعلاً — لمنع إعادة سحبها في كل تشغيل.
- **الأوامر الطويلة بـ`python -c` تفشل في PowerShell** — يأكل علامات
  التنصيص المزدوجة والـescape، وأحياناً يعلّق منتظراً إغلاقاً لن يأتي
  (`Ctrl+C` للخروج). اكتبها في ملف `.py`.
- **`solve_missing.py` و`match_draws.py` مؤرشفان** — فيهما نسخة يدوية
  من الجدول الرسمي؛ عند إحيائهما لموسم آخر حدّث ثابت `OFFICIAL` أولاً.
- **`i18n.py` هو المرجع الوحيد للنصوص** — أي نص جديد يُضاف هناك
  باللغتين قبل استخدامه في سكربتات التوليد.
- **الروابط داخل `en/`** — الشعارات فقط تحتاج `../` إضافية؛ روابط
  `clubs/` و`matches/` تشير لـ`en/clubs/` و`en/matches/` تلقائياً.

---

## أخطاء شائعة أقع بها (للانتباه)

- أنسى `python` قبل اسم الملف بالتيرمنال
- أكتب كود بايثون في التيرمنال بدل الملف
- أنسى `Ctrl+S` بعد التعديل ثم أتساءل لماذا لم يتغير شيء
- التيرمنال يقف بمجلد خاطئ (`cd` مطلوب) — حُلّت بفتح المجلد بـVS Code
- ألصق أمرين معاً في PowerShell فيلتصقان
- ألصق السطر الجديد **فوق** القديم بدل استبداله (ينتج SyntaxError)
- أضع سطرين بايثون على سطر واحد (ينتج SyntaxError)
- أفتح رابط موقع في التيرمنال بدل المتصفح

---

## الخطوات التالية (بالترتيب المتفق عليه)

### 1. الأندية المفضلة — طلب المستخدم
المستخدم يختار أنديته، وتبويب خاص يعرض: نتائجهم · مبارياتهم القادمة
· هدافيهم. يحتاج `localStorage` ليبقى الاختيار محفوظاً.

**هذه أقوى ميزة متبقية** — تحوّل الموقع من مرجع يُزار مرة إلى شيء
يُتابَع أسبوعياً. والموسم بدأ فعلاً، فالمحتوى موجود.

### 2. أسماء اللاعبين العربية
289 لاعباً في `players_arabic.csv` للأردني وحده، وعمود `player_ar`
**فارغ بالكامل**. قسم الهدافين يعرض `Sisa` و`A. Al Haj` في موقع عربي.

**الطريق الذكي:** الموقع يعرض 10 هدافين لكل دوري/موسم و12 لكل نادٍ.
ترجمة **أعلى 25 هدافاً** تغطي كل ما يظهر فعلياً.

يحتاج تعديل `make_site3.py` و`make_clubs.py` و`discipline.py` لقراءة
`player_ar` مع ارتداد للإنجليزي (`make_matches.py` يقرأه بالفعل).

⚠️ **لا تخمّن اسماً** — اتركه فارغاً، فالإنجليزي أفضل من العربي الخطأ.

### 3. زر رجوع يستخدم تاريخ المتصفح
حالياً "رجوع للرئيسية" فقط. الدخول من صفحة نادٍ إلى مباراة لا يسمح
بالعودة للنادي. الحل: `history.back()` — سطر واحد.

### 4. النصوص العربية داخل الداتا تظهر في النسخة الإنجليزية
- ملاحظات التصحيح في `match_corrections.csv` (`هدف د.13 ملغي...`)
- مصادر `manual_matches.csv`

يحتاج عمودَي `note_en` و`source_en`، أو قبولها كما هي.

### 5. التحديث التلقائي — صار مُلحّاً
الموسم بدأ، والنتائج تتغير أسبوعياً. سكربت يشغّل السلسلة كاملة،
وGitHub Actions ينفّذه يومياً.

⚠️ يجب أن يستدعي: `fetch_matches2` + `fetch_upcoming` ← `apply_merges`
← `apply_exclusions` ← `apply_manual` ← `apply_corrections` ←
`make_site3` + `make_clubs` + `make_matches`.

### 6. أرشفة الأهداف الملغاة
`fix_goals.py` يحذف الأهداف الملغاة نهائياً. الأفضل نقلها إلى جدول
`cancelled_goals` لعرضها في صفحة المباراة ("هدف ملغى د.13").

⚠️ الحالتان الحاليتان **ضاعتا بالفعل** — الحل يفيد المستقبل فقط.

### 7. شعار نفط البصرة
الشعار الحالي بخلفية زرقاء (`clean_logos_all.py` يزيل الأبيض فقط).
يحتاج نسخة PNG بخلفية شفافة.

### 8. تفعيل H2H للأردني والعراقي — معلّق
السعودي مفعّل ومتحقق منه. **العراقي أصبح مؤهلاً** بعد اكتمال 2025.
الأردني يحتاج تحققاً من نظام 3 مراحل (لا أثر عملي حالياً).
التفعيل بتغيير قيمة واحدة في `H2H_ENABLED` بـ`tiebreak.py`.

### 9. توسيع الدوريات
خطة المستخدم للمدى الأبعد. **لا تبدأ قبل أن يكون الأساس صلباً** —
كل دوري جديد يحتاج تحققاً خارجياً وأسماء بلغتين وشعارات ومتابعة.

### 10. إعادة تنظيم المجلدات
`data/` و`scripts/` و`output/` — يحتاج تعديل المسارات في `config.py`.

---

## ✅ أُنجزت (للسجل)

- **دعم اللغتين كاملاً** — `i18n.py` + بنية `en/` + ~4000 صفحة
- **الأسماء الإنجليزية الرسمية** — 17 تصحيحاً من 58 نادياً
- **المباريات القادمة** — `fetch_upcoming.py`، 318 مباراة
- **الصفحة الرئيسية** — قادمة ← نتائج ← بطاقات الدوريات ← الجداول
- **موسم 2026-27** — السعودي والعراقي، مع الأندية الصاعدة الخمسة
- **واجهة صفحة النادي** — طي القوائم، فلترة بالنتيجة، ربط الأسماء،
  المباريات القادمة أولاً
- **عرض البطاقات والتبديلات** — قائمة موحدة مرتبة بالدقيقة
- **إحصائيات الانضباط** — `discipline.py`
- **العراقي 2025** — 12 قراراً، مطابق للرسمي 20/20 (درس 19)
- **الصفحات التفصيلية** — 63 نادياً و1960 مباراة، تنقّل بالاتجاهين
- **إكمال مواسم 2024** — السعودي 306 والعراقي 380
- **التحقق الخارجي** — ستة جداول من ستة
- **تصميم الموبايل** — يعمل جيداً؛ `viewport` موجود والجدول واضح
- **تنظيف الملفات** — 29 سكربتاً بالجذر و14 بالأرشيف

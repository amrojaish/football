# صفحة الدوريات — مواصفات إعادة التصميم

> قرارات محسومة بعد نقاش 3 سبتمبر. **ليست اقتراحات.**
> اقرأ README.md أولاً — خصوصاً درس 4 ومبدأ 17 ودرس 22.

---

## المشكلة

`leagues.html` الحالية: ثلاث قوائم منسدلة متداخلة، والزائر يتخذ
ثلاثة قرارات **قبل أن يرى أي شيء**، وكثير من التركيبات ينتهي
بـ"لا بيانات".

**المبدأ الجديد:** الأعماق تصير صفحات لا قوائم. كل ضغطة تعطي
نتيجة مرئية فوراً.

---

## البنية

```
leagues.html
   ثلاثة أعلام: الأردن · العراق · السعودية
        ↓ ضغطة
صفحة البلد
   بطولات البلد — الدوري فقط حالياً
   ⚠️ التصميم يترك مكاناً لبطولات أخرى (كأس) بلا إعادة بناء
        ↓ ضغطة
صفحة الدوري/الموسم
   منسدلة الموسم فوق + أربعة تبويبات
```

---

## صفحة الدوري — القلب

### منسدلة الموسم
- أعلى الصفحة، تعرض **الموسم الحالي افتراضياً**
- الاختيار يفتح صفحة الموسم المطلوب (خمسة مواسم متاحة)

### التبويبات — أربعة
```
الترتيب · المباريات · الهدّافون · إحصائيات
```

⚠️ **صفحة واحدة بتبويبات جافاسكربت، لا صفحات منفصلة.** المحتوى
   كله محمَّل مسبقاً، فالتنقل بين التبويبات **فوري بلا انتظار**.
   هذا قرار مقصود: صفحات منفصلة تعني طلب سيرفر عند كل ضغطة.

⚠️ الرابط لا يتغيّر بين التبويبات. مقبول — الأولوية للسرعة، وليس
   لإضافة روابط جديدة (12 ألف صفحة و726 مفهرَسة فقط).

---

## تبويبا الإحصائيات — انتبه للقيد

`player_stats` **متفاوت التغطية حسب (دوري, موسم) لا حسب الدوري
وحده** (درس 4، محدَّث 6 سبتمبر) — كان يُظن قيداً على مستوى الدوري
(السعودي فقط)، لكن الفحص المباشر أثبت العكس: الإماراتي مثلاً عنده
صفوف حقيقية بموسمَي 2023 و2024 وصفر تماماً بـ2025 (غياب مزوّد
جزئي، لا فجوة سحب — راجع بند مفتوح "صانعو الأهداف" بالـREADME).
العراقي/الأردني/المغربي: صفر كلياً كل المواسم (بلا `lineup_players`
أصلاً — مبدأ 4). التبويبان:

**تبويب "إحصائيات الفرق" — محسوب من النتائج، للسبعة كلهم، تغطية 100%**
- أقوى هجوم · أقوى دفاع
- أطول سلسلة انتصارات / بلا هزيمة
- أكثر التعادلات · فارق الأهداف

**تبويب "إحصائيات اللاعبين" — الهدافون (من `goals`، السبعة كلهم)
+ بطاقات تفصيلية من `player_stats` (تقييم/تصديات/تمريرات حاسمة)
حيثما توفّرت فعلياً لهذا (دوري, موسم) بالضبط**

⚠️ **لا رسالة "غير متاح" ولا قسم فارغ** — بطاقات `player_stats`
   التفصيلية تختفي كلياً لو الاستعلام رجع فارغاً لهذا الموسم
   بالذات (مبدأ 17: الإخفاء أصدق من الفراغ؛ درس 1: القيد يُبنى
   على نتيجة استعلام فعلي لا قائمة أسماء دوريات ثابتة). قائمة
   الهدافين تملأ التبويب دائماً لأنها لا تعتمد على `player_stats`.

---

## قيود التنفيذ

- **`make_leagues.py` هو الأساس** — يُعاد استعماله ويُبنى عليه.
  لا بناء من الصفر؛ الجداول والهدّافون مولَّدان فيه أصلاً.
- **كل النصوص من `i18n.py`** باللغتين. لا نص مكتوب حرفياً.
- **كل الألوان من `theme.py`.** لا لون حرفي.
- **الصفحات بلغتين** — النسخة الإنجليزية تحت `en/`، وروابطها
  تحترم اللغة (لا ترتد للعربي — راجع بادئة `UPL` في
  `search_view.py`).
- **الشريط السفلي** على كل صفحة جديدة (`navbar.py`)، وأيقونة
  "الدوريات" مضيئة (`active="leagues"`).
- **`make_sitemap.py`** يشمل الصفحات الجديدة.
- ⚠️ **المولّدات تكتب ولا تحذف** (درس 22): الصفحات القديمة تبقى
  يتيمة. شغّل `clean_orphans.py --check` بعد التوليد.

---

## الترتيب المطلوب

1. **اقترح البنية والمسارات أولاً — بلا كود.** ما أسماء الملفات،
   وما مسارات الصفحات، وكيف تتفرّع باللغتين. ننتقش ونتفق.
2. بعد الاتفاق: التنفيذ خطوة خطوة، وفحص بعد كل خطوة.
3. **لا رفع.** التوقف عند الاكتمال، والرفع قرار صاحب المشروع.
